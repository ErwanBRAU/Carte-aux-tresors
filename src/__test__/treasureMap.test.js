// const { TreasureMap } = require('../treasureMap');

// const treasureMap = new TreasureMap();

describe('Treasure Map class', () => {
  afterEach(() => {
    jest.resetAllMocks();
  });

  describe('treasureMapResolution', () => {
    afterEach(() => {
      jest.resetAllMocks();
    });
  });
});
